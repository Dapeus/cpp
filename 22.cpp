#include<bits/stdc++.h>

using namespace std;

vector<string> generateParenthesis(int n) {
    vector<string> ret;
    for(int i = 0; i < n; i++) {
        
    }
}

int main()
{
    int n;
    cin>>n;
    generateParenthesis(n);
    system("pause");
    exit(0);
}