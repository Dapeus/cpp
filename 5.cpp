#include<bits/stdc++.h>

using namespace std;

string longestPalindrome(string s) {
    int n = s.length();
    for(int i = 0; i < n; i++) {
        
    }
}

int main()
{
    string s;
    cin>>s;
    longestPalindrome(s);
    system("pause");
    exit(0);
}